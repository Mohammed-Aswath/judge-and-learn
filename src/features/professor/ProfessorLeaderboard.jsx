import React from 'react';
import { Typography, Box } from '@mui/material';

const ProfessorLeaderboard = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Leaderboard
      </Typography>
      <Typography variant="body1">
        Leaderboard monitoring will be implemented here.
      </Typography>
    </Box>
  );
};

export default ProfessorLeaderboard;